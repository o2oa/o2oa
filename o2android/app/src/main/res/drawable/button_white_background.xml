<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 形状的填充色 -->
    <solid android:color="@android:color/white" />
    <!-- 圆角，你也可以对不同的角设置不同的数值 -->
    <corners android:radius="5dp" />
    <!-- 边框宽度和颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/z_color_split_line" />
</shape>