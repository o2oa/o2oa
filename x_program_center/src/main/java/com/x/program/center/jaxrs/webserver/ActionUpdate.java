package com.x.program.center.jaxrs.webserver;

import com.x.base.core.project.server.WebServer;

public class ActionUpdate {
	public WebServer execute(String name, WebServer wrapIn) throws Exception {
//		WebServers webServers = WebServers.load();
//		WebServer server = null;
//		for (WebServer o : webServers) {
//			if (Objects.equals(o.getName(), name)) {
//				server = o;
//			} else {
//				if (Objects.equals(o.getName(), wrapIn.getName())) {
//					throw new Exception("webServer{name:" + name + "} already existed.");
//				}
//			}
//		}
//		if (null == server) {
//			throw new Exception("webServer{name:" + name + "} not existed.");
//		}
//		wrapIn.copyTo(server);
//		webServers.sort();
//		WebServers.store(webServers);
//		ThisApplication.webServers = webServers;
//		return server;
	return null;
	}

}
