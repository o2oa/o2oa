package com.x.program.center.jaxrs.webserver;

import com.x.base.core.project.server.WebServer;

public class ActionCreate {
	public WebServer execute(WebServer wrapIn) throws Exception {
//		WebServers webServers = WebServers.load();
//		for (WebServer o : webServers) {
//			if (StringUtils.equalsIgnoreCase(o.getName(), wrapIn.getName())) {
//				throw new Exception("server{name:" + wrapIn.getName() + "} already existed.");
//			}
//		}
//		webServers.add(wrapIn);
//		webServers.sort();
//		WebServers.store(webServers);
//		ThisApplication.webServers = webServers;
//		return wrapIn;
		return null;
	}

}
