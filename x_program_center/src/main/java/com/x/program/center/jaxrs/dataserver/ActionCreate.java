package com.x.program.center.jaxrs.dataserver;

import com.x.base.core.project.server.DataServer;


public class ActionCreate {

	public DataServer execute(DataServer wrapIn) throws Exception {
//		DataServers dataServers = DataServers.load();
//		for (DataServer o : dataServers) {
//			if (StringUtils.equalsIgnoreCase(o.getName(), wrapIn.getName())) {
//				throw new Exception("server{name:" + wrapIn.getName() + "} already existed.");
//			}
//		}
//		dataServers.add(wrapIn);
//		dataServers.sort();
//		DataServers.store(dataServers);
//		return wrapIn;
		return null;
	}

}
