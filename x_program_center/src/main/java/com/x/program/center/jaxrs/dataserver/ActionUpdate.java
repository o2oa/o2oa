package com.x.program.center.jaxrs.dataserver;

import com.x.base.core.project.server.DataServer;


public class ActionUpdate {

	public DataServer execute(String name, DataServer wrapIn) throws Exception {
//		DataServers dataServers = DataServers.load();
//		DataServer server = null;
//		for (DataServer o : dataServers) {
//			if (Objects.equals(o.getName(), name)) {
//				server = o;
//			} else {
//				if (Objects.equals(o.getName(), wrapIn.getName())) {
//					throw new Exception("dataServer{name:" + name + "} already existed.");
//				}
//			}
//		}
//		if (null == server) {
//			throw new Exception("dataServer{name:" + name + "} not existed.");
//		}
//		wrapIn.copyTo(server);
//		dataServers.sort();
//		DataServers.store(dataServers);
//		return server;
		return null;
	}

}
